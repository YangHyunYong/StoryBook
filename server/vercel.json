{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/stability/:path*",
      "destination": "/api/stability/:path*"
    }
  ]
}

